version: 2.1

jobs:
  test:
    machine: true
    environment:
      TERM: dumb
    steps:
      - gcp-auth/authenticate-gcp
      - run: gcloud config get-value account

workflows:
  main:
    jobs:
      - test:
          name: build
